exclude a column with cut

cut -f5 --complement

Show all columns except 5th. This might help you save some typing if you
are trying to exclude some columns from the output.

Alternatives

Terminal - Alternatives
awk '{ $5=""; print }' file

Here's an awk alternative, for those lacking the version of cut with the
--complement argument.

Setup a persistant SSH tunnel w/ pre-shared key authentication

Terminal - Setup a persistant SSH tunnel w/ pre-shared key authentication
autossh -f -i /path/to/key -ND local-IP:PORT User@Server

User: fotoflo
Setup a persistant SSH tunnel w/ pre-shared key authentication

This creates a persistent ssh -i /path/to/key -ND local-IP:PORT
User@Server connection. You may have to install autossh. -f puts in daemon
mode. if you are having trouble, try it without -f.

for some reason it doesnt work in cygwin

Comment by fotoflo 40 weeks and 5 days ago


Copy your SSH public key on a remote machine for passwordless login.

Terminal - Copy your SSH public key on a remote machine for passwordless
login.
cat ~/.ssh/*.pub | ssh user@remote-system 'umask 077; cat
>>.ssh/authorized_keys'

User: DASKAjA
Copy your SSH public key on a remote machine for passwordless login.

Should run on any system with ssh installed.

ssh-copy-id is much simpler.

Comment by nomad 86 weeks and 2 days ago

ssh-copy-id isn't available on all systems, i.e. Mac OS X

Comment by DASKAjA 86 weeks and 2 days ago

